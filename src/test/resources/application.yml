spring:
  application:
    name: cockroach-playground
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:26257/playground
    username: root
    password:
    driver-class-name: org.postgresql.Driver
server:
  port: 8080